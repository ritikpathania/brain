[package]
name = "brain-fitness-tests"
version = "0.1.0"
edition = "2021"
description = "Automated Architecture Fitness Tests enforcing Brain Constitutional Invariants"

[dependencies]
cargo_metadata = "0.18"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
walkdir = "2.4"

[dev-dependencies]
pretty_assertions = "1.4"
