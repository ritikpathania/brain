[package]
name = "brain-config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde.workspace = true
toml.workspace = true
brain-domain = { path = "../brain-domain" }
brain-core = { path = "../brain-core" }
