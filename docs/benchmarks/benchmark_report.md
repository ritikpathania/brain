# Performance & Resource Validation Baseline Report

**Schema Version:** `1.1`  
**Recorded Date:** `2026-08-07`  
**Profile:** `release`  

---

## 1. Measured Resource & Latency Metrics

| Metric | Measurement Methodology | Recorded Baseline | CI Gate Threshold |
| :--- | :--- | :--- | :--- |
| **Cold Startup Latency** | `scripts/perf_runner.sh` (socket health polling) | **497 ms** | $+10\%$ max delta |
| **Sampled Steady-State RSS** | `scripts/perf_runner.sh` (`ps` memory footprint) | **16,080 KB** (~16.08 MB) | $+5\%$ max growth |
| **Idle CPU Utilization** | `scripts/perf_runner.sh` (`ps` CPU load) | **0.0 %** | $1.0\%$ max absolute |
| **P95 Frame Draw Time** | `cargo bench -p brain-tui` (`frame_draw_empty_120x40`) | **108.12 µs** | Criterion baseline |
| **Streaming Tokenizer Throughput**| `cargo bench -p brain-tui` (`tokenizer_feed_chunk`) | **516.59 ns** | Criterion baseline |

---

## 2. Telemetry JSON Schema (`v1.1`)

Generated by `./scripts/perf_runner.sh target/perf_baseline.json`:

```json
{
  "schema_version": "1.1",
  "timestamp": "2026-08-07T03:15:22Z",
  "git": {
    "commit": "691629fe24a79ab0042d689dc045f44d1173f565",
    "branch": "main",
    "dirty": false
  },
  "environment": {
    "os": "Darwin",
    "architecture": "arm64",
    "rust_version": "rustc 1.84.0",
    "profile": "release"
  },
  "metrics": {
    "cold_startup_ms": 497,
    "sampled_rss_kb": 16080,
    "idle_cpu_percent": 0.0
  }
}
```

---

## 3. Automated CI Regression Evaluator

The `scripts/check_perf.py` runner evaluates new benchmark outputs against `target/perf_baseline.json`:

```bash
python3 scripts/check_perf.py target/perf_baseline.json target/perf_current.json
```

Exits with `0` on success and `1` on performance regression failures.
