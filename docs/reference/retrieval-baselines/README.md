# Archived Retrieval Baselines

This directory contains versioned baseline reports of the retrieval quality and performance benchmarks.

Each report is a structured, versioned JSON file generated by `eval_cli` (e.g. `v0.7.0.json`) adhering to a standardized schema. These baselines are used as gating checks in CI to detect quality regressions.
