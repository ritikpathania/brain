# Claude Code vs. Brain TUI Mechanical Visual Gap Matrix (v2)

> **CANONICAL MECHANICAL AUDIT**: Mechanical comparison of Claude Code Visual Contract vs. Brain TUI baseline implementation (`1c3df23a059b5e7a545f63af5f8b4f08389d2767`).
> **CLASSIFICATION TAXONOMY**: `MATCH`, `CLOSE`, `MICRO_DELTA`, `STRUCTURAL_DELTA`, `MISSING`, `UNKNOWN`.
> **EVIDENCE TAGS**: `[VERIFIED_CLAUDE]`, `[VERIFIED_BRAIN]`, `[INFERRED]`, `[PROPOSED_ADAPTATION]`.

---

## 1. Mechanical Component & Layout Matrix

| Component / Rule | Claude Code Reference Behavior | Brain Current Baseline Behavior | Classification | Severity | Evidence / Code Location | Architectural Cause |
|---|---|---|---|---|---|---|
| **Global App Shell** | Borderless floor (`Color::Reset`). Split into scrollable canvas (`flexGrow: 1`) + pinned bottom (`flexShrink: 0`). `[VERIFIED_CLAUDE]` | Boxed viewports surrounded by `Rounded` borders. Layout divided into Header, Sidebar, Chat, Prompt, Status. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `renderer.rs` lines 105–170 (`compute_layout`) | Brain wraps main viewports inside explicit Ratatui `Block::default().borders(Borders::ALL)`. |
| **Home Landing Page** | Centered Clawd logo + tagline at top of transcript stream. Prompt pinned at bottom. Scrollable empty area above prompt. `[VERIFIED_CLAUDE]` | Logo + Try commands list + boxed prompt + boxed status bar. Prompt anchored at ~67% height on tall terminals. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `renderer.rs` lines 180–200, `home/mod.rs` | Brain treats Home as a distinct screen mode (`AppLayoutMode::Welcome`) with custom vertical positioning logic. |
| **Prompt Input Box** | Boxed input with `Rounded` borders. Focused border turns brand orange (`rgb(215,119,87)`). 1-line margin above. `[VERIFIED_CLAUDE]` | Boxed input with `Rounded` borders. Focused border turns active theme primary token. `[VERIFIED_BRAIN]` | `MICRO_DELTA` | Low | `prompt.rs`, `theme/style.rs` | Theme token mapping uses generic primary token instead of brand accent token. |
| **Conversation Viewport** | Edge-to-edge canvas on terminal floor (`Color::Reset`). Zero outer container borders. `[VERIFIED_CLAUDE]` | Container box with `Rounded` borders (`Borders::ALL`) surrounding conversation messages. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `widgets/chat.rs` lines 40–90 | `ChatView` wraps message list in Ratatui `Block::default().borders(Borders::ALL)`. |
| **User Message Block** | Left accent pillar (`▎`), prompt icon (`❯`), bold user text, right-aligned timestamp. 1-line gap above/below. `[VERIFIED_CLAUDE]` | Indented text block with `User:` prefix inside boxed viewport. `[VERIFIED_BRAIN]` | `MICRO_DELTA` | Medium | `widgets/chat.rs` lines 120–180 | Rendering logic uses text prefix (`User:`) rather than vertical pillar graphic (`▎`). |
| **Assistant Message Block**| Edge-to-edge text stream. Markdown code fences filled with surface color (`bgSecondary`). `[VERIFIED_CLAUDE]` | Text block inside boxed viewport with assistant header. `[VERIFIED_BRAIN]` | `CLOSE` | Low | `widgets/chat.rs` lines 200–260 | Markdown renderer operates cleanly; container box creates visual boundary overhead. |
| **Reasoning Progress** | Single-line Braille dot spinner (`⠋ Thinking...`) with alternating shimmer colors. Auto-collapses on token. `[VERIFIED_CLAUDE]` | Multi-row step checklist (`○ Pending`, `● Active`, `✓ Completed`) taking 4 rows. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | Medium | `reasoning_progress.rs` lines 30–80 | `ReasoningProgressWidget` renders multi-step list instead of single-line shimmer spinner. |
| **Streaming Output** | Typewriter token stream updating at ~60fps without moving manual scroll position or canvas jumping. `[VERIFIED_CLAUDE]` | Monotonic stream chunk delivery into typewriter queue. `[VERIFIED_BRAIN]` | `MATCH` | None | `streaming_rendering_tests.rs`, `state.rs` | Typewriter queue and monotonic sequence handling are functionally identical. |
| **Evidence & Memory Cards**| Collapsible single-line summary (`▶ 3 memories recalled`), expanding on demand. `[VERIFIED_CLAUDE]` | Full-width bordered cards showing scores, match badges, weights, and provenance paths. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `widgets/evidence_card.rs` | Brain renders dense persistent cards for relational memory instead of inline collapsible chips. |
| **Command Palette (`Ctrl+K`)**| Floating overlay with single-line hint footer (`↑↓ navigate  tab select  esc close`). Dynamic height clamp. `[VERIFIED_CLAUDE]` | Centered modal dialog box with `Rounded` borders and fixed max height. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `widgets/palette.rs` lines 50–120 | `PaletteWidget` uses centered modal box layout instead of floating prompt dropdown. |
| **Slash Completion Dropdown**| Floating dropdown attached right above prompt line with single-line hints. `[VERIFIED_CLAUDE]` | Bordered completion box overlaid above prompt line. `[VERIFIED_BRAIN]` | `CLOSE` | Low | `widgets/completion.rs` | Positioning is anchored correctly; visual frame uses boxed borders instead of subtle floating rule. |
| **Workspace Sidebar** | Ephemeral or toggled via command; full width dedicated to stream by default. `[VERIFIED_CLAUDE]` | Permanent 22-column sidebar locked on the left in Workspace mode. `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `renderer.rs` lines 220–250, `sidebar.rs` | Workspace layout hardcodes 22-column sidebar split by default. |
| **Status Footer Bar** | Single-line borderless bar at bottom row (`y = height - 1`). Muted hints left, status right. `[VERIFIED_CLAUDE]` | 1-row bordered panel with 4 explicit metric boxes (`READY`, `DAEMON`, `TOKENS`, `HELP`). `[VERIFIED_BRAIN]` | `STRUCTURAL_DELTA` | High | `status_footer.rs` lines 30–90 | `StatusFooterWidget` draws boxed metric slots instead of borderless text hints. |
| **Cancellation Presentation**| Stream halts cleanly; prompt input restores focus instantly with soft `Interrupted` badge. `[VERIFIED_CLAUDE]` | Boxed error message ("Request cancelled"). `[VERIFIED_BRAIN]` | `MICRO_DELTA` | Medium | `widgets/chat.rs`, `state.rs` | Cancellation sets error-style message block instead of soft inline badge. |

---

*This document establishes the official v2 mechanical gap matrix between Claude Code and Brain TUI.*
